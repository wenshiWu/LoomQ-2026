# Public evaluator baseline: no third-party dependencies.
# Our L1 uses a dependency-free state-vector simulator (quantum_engine.py),
# so no extra packages are required. L2 uses the standard library + HTTP.
